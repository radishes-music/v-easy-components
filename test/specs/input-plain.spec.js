import Input from '@packages/input/index'

describe('Input', () => {
  it('input v-model', () => {})

  it('input attributes max-width and error-options', () => {})

  it('input attributes disabled', () => {})

  it('input attributes readonly', () => {})

  it('input attributes message', () => {})

  it('input type RegExp inspect ', () => {})

  it('input native type number or text ot password', () => {})

  it('input native type is number step', () => {})

  it('input verify target type', () => {})

  it('input option max or min', () => {})
})
